version: 2.1
orbs:
  node: circleci/node@1.1.6
jobs:
  build:    
    docker:
      — image: circleci/node:7.10
    steps:
      - checkout
      - run: npm install


workflows:
    build:
      jobs:
        - build