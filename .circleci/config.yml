version: 2.1
orbs:
  node: circleci/node@1.1.6
jobs:
  build:
    executor:
      node:
        docker:
          - image: circleci/node:10.16
    steps:
      - checkout
      - node/with-cache:
          steps:
            - run: npm install
workflows:
  build:
    jobs:
      - build